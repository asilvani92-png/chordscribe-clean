[build]
builder = "DOCKER"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -lc 'python -m uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[env]
PYTHONUNBUFFERED = "1"
